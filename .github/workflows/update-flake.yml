name: Update Flake

on:
  schedule:
    # Run every night at 2 AM UTC
    - cron: "0 2 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  actions: read

jobs:
  update-flake:
    runs-on: ubuntu-latest
    outputs:
      branch-name: ${{ steps.create-branch.outputs.branch-name }}
      updated: ${{ steps.check-changes.outputs.updated }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Prepare runner
        uses: ./.github/actions/prepare-runner
        with:
          ssh-private-key: ${{ secrets.PRIVATE_REPO_SSH_KEY }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update flake inputs
        run: nix flake update

      - name: Check for changes
        id: check-changes
        run: |
          if git diff --quiet flake.lock; then
            echo "updated=false" >> $GITHUB_OUTPUT
            echo "No updates available"
          else
            echo "updated=true" >> $GITHUB_OUTPUT
            echo "Updates available"
          fi

      - name: Create and push update branch
        if: steps.check-changes.outputs.updated == 'true'
        id: create-branch
        run: |
          BRANCH_NAME="update-flake-$(date +%Y%m%d-%H%M%S)"
          echo "branch-name=$BRANCH_NAME" >> $GITHUB_OUTPUT

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH_NAME"
          git add flake.lock
          git commit -m "chore: update flake.lock"
          git push origin "$BRANCH_NAME"

  verify-update:
    needs: update-flake
    if: needs.update-flake.outputs.updated == 'true'
    uses: ./.github/workflows/ci.yml
    with:
      ref: ${{ needs.update-flake.outputs.branch-name }}

  handle-result:
    needs: [update-flake, verify-update]
    if: needs.update-flake.outputs.updated == 'true' && always()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Merge to main if successful
        if: needs.verify-update.result == 'success'
        env:
          BRANCH_NAME: ${{ needs.update-flake.outputs.branch-name }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git fetch origin "$BRANCH_NAME"
          git merge --ff-only "origin/$BRANCH_NAME" -m "chore: update flake.lock"
          git push origin main

          # Delete the branch
          git push origin --delete "$BRANCH_NAME"
          echo "✅ Successfully merged and deleted branch $BRANCH_NAME"

      - name: Create PR if verification failed
        if: needs.verify-update.result != 'success'
        env:
          BRANCH_NAME: ${{ needs.update-flake.outputs.branch-name }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create \
            --base main \
            --head "$BRANCH_NAME" \
            --title "chore: update flake.lock" \
            --body "Automated flake update failed verification. Please review the changes and CI results." \
            --reviewer "${{ github.repository_owner }}"
          echo "⚠️ Created PR for manual review due to verification failure"

      - uses: crazy-max/ghaction-import-gpg@v6
        if: needs.verify-update.result != 'success'
        with:
          gpg_private_key: ${{ secrets.ACTIONS_GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.ACTIONS_GPG_PASSPHRASE }}
          git_user_signingkey: true
          git_commit_gpgsign: true

      - name: Create PR with fixes
        if: needs.verify-update.result != 'success'
        uses: peter-evans/create-pull-request@v8
        env:
          GH_REPO: ${{ github.repository }}
          GH_TOKEN: ${{ github.token }}
        with:
          commit-message: "chore: update flake.lock"
          committer: CI <kuzzz99@gmail.com>
          branch: "${{ needs.update-flake.outputs.branch-name }}"
          delete-branch: true
          title:  "chore: update flake.lock"
          reviewers: "kuznetsss"
